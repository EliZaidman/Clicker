using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.EventSystems;

public class AnimationScript : MonoBehaviour, IPointerClickHandler, IPointerEnterHandler
{
    GameObject button;

    public void OnPointerClick(PointerEventData eventData) //OnClick
    {
       
    }

    public void OnPointerEnter(PointerEventData eventData) //OnHover
    {
       
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (button) ;
    }
}
